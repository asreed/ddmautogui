<UserControl x:Class="DDMAutoGUI.dispenseSteps.dispenseStep1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DDMAutoGUI.dispenseSteps"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Reference Images"
                  Grid.Column="0" 
                  Margin="0"
                  Padding="6"
                  Style="{DynamicResource stepBox}">

            <Image HorizontalAlignment="Center" 
                   VerticalAlignment="Top" 
                   Width="450"
                   Source="/images/dispenseDiagrams/sn_example.jpg"/>
        </GroupBox>

        <GroupBox Header="Step Instructions"
                  Grid.Column="1"
                  Margin="3,0,0,0"
                  Style="{DynamicResource stepBox}">

            <Grid Margin="6,12,30,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Column="0"
                       Grid.Row="0"
                       Margin="0"
                       Padding="0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       Content="1.1"/>

                <StackPanel Grid.Column="1"
                            Grid.Row="0"
                            Orientation="Vertical">

                    <TextBlock TextWrapping="Wrap"
                               Foreground="Black"
                               Text="Enter the serial number printed on the magnet ring."/>

                    <TextBox Name="snTextBox" 
                             Text="" 
                             Margin="0,12,0,0"
                             VerticalAlignment="Top"
                             Style="{DynamicResource stepTextBox}"
                             TextChanged="snTextBox_TextChanged"/>

                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
