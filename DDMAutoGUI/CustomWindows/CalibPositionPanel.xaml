<UserControl x:Class="DDMAutoGUI.CustomWindows.CalibPositionPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DDMAutoGUI.CustomWindows"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="450">

    <Grid>

        <StackPanel Orientation="Vertical" Margin="12">
            
            <Grid Margin="4" Background="WHite">
                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <!--<Setter Property="Background" Value="WhiteSmoke"/>-->
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="J1 Position"/>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="J2 Position"/>


                <TextBlock Grid.Row="0" Grid.Column="1" Text="-" x:Name="j1PosTxb"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="-" x:Name="j2PosTxb"/>

            </Grid>
            
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Vertical">
                    <Button Style="{StaticResource BlackButton}" Content="Calibrate position" x:Name="CalPosBtn" Click="CalPosBtn_Click"/>
                    <ProgressBar x:Name="CalPosPrg"/>
                </StackPanel>
            </StackPanel>
            
        </StackPanel>

    </Grid>
</UserControl>
